:8080

# 先把 /api/* 轉給後端（不剝掉 /api，因為你 FastAPI 路由本來就寫 /api/...）
handle /api/* {
    reverse_proxy 127.0.0.1:8000
}

# 其他都當前端靜態檔
handle {
    root * ./frontend
    try_files {path} /index.html
    file_server
}
